<ls:StateMachine xmlns:ls="clr-namespace:ls;assembly=Code"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!--
		In here you will be able to extend or override the original States creates by Larian Studios.
		As well as extend or override the States of previously loaded mods.

		Your states will contain your custom UI Pages or Pages contained in previously loaded mods or created by Larian Studios.

		This StateMachine gets loaded when the Keyboard and mouse mode is activated.
	-->
	
    <ls:StateMachine.States>
        <!--STATE: MainMenu-->
        <ls:State Name = "MainMenu" ModType="Extend">
            <ls:State.Widgets>
                <ls:StateWidget Filename="TestModHub.xaml" Layer="HUD"/>
            </ls:State.Widgets>
        </ls:State>

        <!--STATE: PlayerHUD-->
        <ls:State Name = "PlayerHUD" Layout = "Player" Owner = "Player" ModType="Override">
            <ls:State.Widgets>
                <ls:StateWidget Filename="DT_HotBar.xaml" Layer="HUDTop"/>
            </ls:State.Widgets>
        </ls:State>

    </ls:StateMachine.States>
    
</ls:StateMachine>